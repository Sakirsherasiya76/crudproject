import React from 'react'

const Main = () => {
  return (
    <div>
      {/* <button onClick={() => localStorage.setItem('items', JSON.stringify("sakir sherasiya"))}>setItem</button> */}
      {/* <button onClick={() => alert(localStorage.getItem(('items')))}>getItem</button> */}
      {/* <button onClick={() => alert(localStorage.removeItem(('items')))}>removeItem</button> */}
      {/* <button onClick={() => alert(localStorage.clear(('items')))}>clear</button> */}


    </div>
  )
}

export default Main;
